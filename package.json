{
  "name": "tcc-etl",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "create": "ts-node-dev --inspect --transpile-only --ignore-watch node_modules src/create.ts",
    "update": "ts-node-dev --inspect --transpile-only --ignore-watch node_modules src/update.ts"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.4"
  }
}
